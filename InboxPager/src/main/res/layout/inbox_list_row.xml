<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="?android:attr/listPreferredItemHeight">

    <TextView android:id="@+id/inbox_list_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@+id/inbox_list_count"
        android:layout_toLeftOf="@+id/inbox_list_count"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_marginStart="10sp"
        android:layout_marginLeft="10sp"
        android:layout_marginTop="4sp"
        android:layout_marginBottom="4sp"
        android:textSize="@dimen/text_size_inbox_item"
        android:maxLines="1"
        android:ellipsize="end"/>

    <TextView android:id="@+id/inbox_list_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="10sp"
        android:layout_marginRight="10sp"
        android:padding="4sp"
        android:maxLines="1"
        android:textSize="@dimen/text_size_inbox_item"
        android:textColor="@color/color_home_bar_background"
        android:background="@drawable/counter_rounded"/>

</RelativeLayout>
